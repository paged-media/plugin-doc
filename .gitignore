# Rust
/target

# TS
node_modules/
dist/
*.tsbuildinfo

# wasm-bindgen build outputs (regenerated by scripts/build-wasm.sh;
# bin/.gitkeep keeps the directory tracked)
packages/doc-bundle/bin/*.wasm
packages/doc-bundle/bin/*.js
packages/doc-bundle/bin/*.d.ts

# clean-room / reference mounts — never committed
references/
.venv/
